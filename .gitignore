# Cloudflare config — contains your D1 database_id; copy wrangler.jsonc.example and fill in your values
wrangler.jsonc
wrangler.demo.jsonc

# Wrangler local dev state (local SQLite, cache)
.wrangler/

# Claude Code project config
.claude/

# Data import/seed scripts — may contain personal ward data
scripts/

# Data import/seed migrations — contain ward member names and event details.
# These live outside migrations/ proper so `wrangler d1 migrations apply` never
# sees them; apply them manually via `wrangler d1 execute --file=migrations/data/XXXX.sql`.
migrations/data/*.sql

# Environment files — never commit credentials or secrets
.env
.env.*

# Data exports — CSV/JSON dumps may contain member data
*.csv
data-export/
exports/

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
